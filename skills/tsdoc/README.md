# TSDoc

Add expert-level inline TSDoc to staged TypeScript code without changing runtime behavior.

## Inputs

- Staged TypeScript diff
- Optional surrounding call sites when symbol behavior is ambiguous

## Includes

- `SKILL.md`
- `agents/openai.yaml`
